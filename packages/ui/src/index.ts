/**
 * @connected/ui — design system: tokens + primitives (theme-aware, responsive, WCAG AA).
 * Consumed by apps/web (and mobile later). Owned by the ui-designer charter.
 *
 * Scaffold only — tokens and components land alongside the web shell (Sprint 0).
 */

export const CONNECTED_UI_PLACEHOLDER = true;
