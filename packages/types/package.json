{
  "name": "@connected/types",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "//": "Consumers get built output, not raw .ts. apps/api runs on plain node, which cannot import TypeScript, and pointing main at src/ also leaves the Next bundler resolving './auth.js' against a file that does not exist. `turbo` builds this before anything that depends on it.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "test": "echo \"[types] no tests yet\""
  },
  "devDependencies": {
    "@connected/config": "workspace:*",
    "eslint": "^9.12.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
