{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "files": [],
  "//": "Only the shared libraries are referenced. The apps are leaves — nothing consumes their build output — and referencing them would force `composite` on their tsconfigs for no benefit.",
  "references": [{ "path": "./packages/types" }, { "path": "./packages/ui" }]
}
