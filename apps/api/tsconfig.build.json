{
  "//": "Build config — drops tests and tooling so dist/ ships only runtime code.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src"
  },
  "exclude": ["src/**/*.test.ts", "src/**/__tests__/**", "vitest.config.ts"]
}
