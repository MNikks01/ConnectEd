---
---

Academic content — publishing, class feed, read tracking, edit/delete, and class-wide notification fan-out (S2-1..S2-4, S2-6). `@connected/api` is deployed rather than published and `@connected/types` is private, so no version bump.
