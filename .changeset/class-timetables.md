---
---

Class timetables (S2-9, FR-ACAD-020): the school uploads an image per class, verified members view it, and each
upload becomes a new version rather than overwriting the last. `@connected/api` is deployed rather than
published and `@connected/web` is private, so no version bump.
