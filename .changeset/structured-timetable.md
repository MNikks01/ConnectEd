---
'@connected/types': minor
---

Adds the structured timetable to the contract (FR-ACAD-021): `uploadTimetableSchema` now takes either an `imageKey` or a set of `periods`, and `TimetableResponse` gains `kind` and `periods`.
