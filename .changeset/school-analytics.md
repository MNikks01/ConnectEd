---
'@connected/types': minor
---

Add the school-analytics DTOs and the `FEATURE_NOT_IN_PLAN` error code (402) — a sibling of
`PLAN_LIMIT_EXCEEDED` for a feature a plan never included, rather than a limit it has reached
(S6-7).
