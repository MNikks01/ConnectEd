---
---

Blocking and reporting endpoints (S4-8): block, unblock, list, and report a post, comment, message or account.
Adds the `report` table and a migration. `@connected/api` is deployed rather than published, so no version bump.
