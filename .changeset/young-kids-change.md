---
---

Notifications with a BullMQ event queue (S1-11), plus fixes for the notification idempotency constraint and a publish that hung when Redis was unreachable. `@connected/api` is deployed rather than published, so no version bump.
