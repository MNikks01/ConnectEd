---
---

Next.js web skeleton with working login (S0-8), and shared auth schemas moved into `@connected/types`. The apps are deployed rather than published, and `@connected/types` is private, so no version bump.
