---
---

Cursor pagination (S2-0b) applied to the lists that grow, with a ceiling on the ones that do not. `@connected/api` is deployed rather than published and `@connected/types` is private, so no version bump.
