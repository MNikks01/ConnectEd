---
---

Direct messages (S4-7, FR-SOC-020/021): 1:1 threads, per-message read state, unread counts, and a rate limit on
sending. `@connected/api` is deployed rather than published, so no version bump.
