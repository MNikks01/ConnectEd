---
---

Syllabus coverage (S2-10, FR-ACAD-030, 031): an allocated teacher records progress per topic, and every verified
member of the class can see how far each subject has got. `@connected/api` is deployed rather than published and
`@connected/web` is private, so no version bump.
