---
---

Adds the school's read-only view of a class's marks (S8-3), and makes the assessment list agree with the marks endpoint about what a school may see. `@connected/web` and `@connected/api` are private, so nothing is published.
