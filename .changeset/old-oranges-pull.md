---
---

Playwright end-to-end suite (S1-12), plus a fix for the blank-optional-field bug it found in the shared school-profile schema and an explicit `RATE_LIMIT_ENABLED` switch. All packages are private or deployed rather than published, so no version bump.
