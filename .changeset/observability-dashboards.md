---
---

The Database, Queue/worker, and Business dashboards, and the metrics they needed: domain-event
fan-out outcomes and latency, queue depth and lag, and Postgres pool occupancy (S5-10). Six new
alert rules follow from them. `@connected/api` is private, so no version bump.
