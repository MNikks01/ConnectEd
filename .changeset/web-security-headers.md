---
---

Security headers on the web app: a nonce-based content security policy, and the frame, referrer and permissions headers that go with it. `@connected/web` is private, so nothing is published.
