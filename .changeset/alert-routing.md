---
---

Alert routing (S3-11): Alertmanager, severity-based routes, grouping, inhibition, per-alert runbook links, and a
CI check that fails the build on an alert with nowhere to go. Configuration only — no package changes.
