---
---

Tag every release to `main` as `release/YYYY-MM-DD`, with a GitHub Release carrying generated
notes, and backfill the two releases that shipped before the convention existed. Also document
that private-package changesets must use empty frontmatter. CI and docs only.
