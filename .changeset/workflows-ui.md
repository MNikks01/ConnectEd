---
---

Leave and complaints in the web app (S3-8, S3-9), plus `GET /me/class-teacher` — a class teacher had no way to
discover the queues they own. `@connected/api` is deployed rather than published and `@connected/web` is
private, so no version bump.
