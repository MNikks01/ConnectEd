---
---

Two carried items: the release tag now annotates with the release commit rather than the merge
subject (S6-9), and a failed integration test dumps the database state before a rerun can destroy
it (S6-11). CI and test infrastructure only.
