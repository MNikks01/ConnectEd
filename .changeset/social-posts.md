---
---

Posts (S4-2, FR-SOC-002/004), the blocking filter every social read shares, and the first rate limit outside
authentication (S4-0b). Adds the `block` table and a migration. `@connected/api` is deployed rather than
published, so no version bump.
