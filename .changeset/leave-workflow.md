---
---

Leave applications (S3-1..S3-6, FR-WF-001..006): a parent applies for a verified child and the class teacher of
that class decides; a teacher applies for themselves and the principal decides. Adds the calendar-date contract
to the API conventions. `@connected/api` is deployed rather than published, so no version bump.
