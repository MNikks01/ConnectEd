---
---

Notices and events (S2-5, FR-ACAD-010..012): school/principal publishing, community reads, read tracking, and
fan-out to every verified member. `@connected/api` is deployed rather than published and `@connected/web` is
private, so no version bump.
